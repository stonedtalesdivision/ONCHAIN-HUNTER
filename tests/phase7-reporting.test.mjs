import test from "node:test";
import assert from "node:assert/strict";
import {buildDisclosureReport} from "../dist/disclosure-report.js";
const pkg={packageId:"ep",findingId:"f",title:"Potential reentrancy",file:"Vault.sol",severity:"high",confidence:.91,categories:["reentrancy"],impact:"asset-loss",exploitability:"direct",scopeConfidence:"exact-revision",attackPath:["attacker-controlled input: recipient","reachable function: withdraw","external call: recipient"],transactionSequence:[{order:1,function:"withdraw",role:"entry",evidence:"external"}],corroborationCount:2,evidence:["external call on recipient"],reviewQuestions:[],validationPlan:["run local test"],repository:"github.com/acme/vault",sourceRevision:"abc",submissionReady:false};
const match={programId:"p",programName:"Vault",programUrl:"https://example",repository:"github.com/acme/vault",scope:"exact-repository",impact:"unverified",severity:"unverified",payoutRoutes:[{network:"Ethereum",asset:"USDC",configured:true,reason:"configured"}],requirements:{pocRequired:"unknown",kycRequired:"unknown",prohibitedActivities:[],impactCategories:[],knownIssueNotes:[],sourceUrl:"https://example"},eligibility:"review-required",reasons:[]};
test("builds a human-review disclosure report",()=>{const r=buildDisclosureReport(pkg,match);assert.equal(r.schemaVersion,"phase-7");assert.equal(r.submissionReady,false);assert.equal(r.validation.status,"not-run");assert.ok(r.reviewBlockers.length>=3);assert.match(r.markdown,/Human review/);});
test("never marks an unvalidated report submission-ready",()=>{const r=buildDisclosureReport(pkg,match,{status:"failed",exitCode:1,command:"forge test",stdout:"",stderr:"failure",safety:{localOnly:true,liveNetworkTraffic:false,explicitExecutionRequired:true}});assert.equal(r.submissionReady,false);assert.match(r.markdown,/Submission is intentionally disabled/);});
