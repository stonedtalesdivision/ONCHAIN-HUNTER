import test from "node:test";import assert from "node:assert/strict";import {matchEvidencePackage,rankBountyMatches} from "../dist/bounty-matcher.js";
const pkg={packageId:"ep",findingId:"f",title:"candidate",file:"Vault.sol",severity:"high",confidence:.9,categories:["reentrancy"],impact:"asset-loss",exploitability:"direct",scopeConfidence:"exact-revision",attackPath:[],transactionSequence:[],corroborationCount:2,evidence:[],reviewQuestions:[],validationPlan:[],repository:"github.com/acme/vault",sourceRevision:"abc",submissionReady:false};
const route=()=>[{network:"Ethereum",asset:"USDC",configured:true,reason:"configured"}];
test("matches only active programs with explicit repository scope",()=>{const ms=matchEvidencePackage(pkg,[{id:"p",name:"Vault",platform:"Immunefi",url:"https://example",status:"active",chains:["Ethereum"],inScope:["Vault"],sourceRepos:["github.com/acme/vault"],fetchedAt:"now"}],route);assert.equal(ms.length,1);assert.equal(ms[0].scope,"exact-repository");assert.equal(ms[0].eligibility,"review-required")});
test("does not present an unlisted repository as bounty eligible",()=>{const ms=matchEvidencePackage(pkg,[{id:"p",name:"Other",platform:"Immunefi",url:"https://example",status:"active",chains:["Ethereum"],inScope:[],sourceRepos:["github.com/acme/other"],fetchedAt:"now"}],route);assert.equal(ms.length,0)});
test("rank prioritizes exact scope",()=>{const a={scope:"repository-not-listed",impact:"matched",severity:"matched"},b={scope:"exact-repository",impact:"unverified",severity:"unverified"};const ranked=rankBountyMatches([a,b]);assert.deepEqual(ranked[0],b);assert.deepEqual(ranked[1],a)});
